# Fly.io configuration for HelpfulBatBot
# Deploy with: flyctl deploy

app = "underworld-helpfulbat"
primary_region = "syd"  # Sydney, Australia

[build]
  dockerfile = "Dockerfile"

[env]
  PORT = "8001"
  CONTENT_UPDATE_FREQUENCY = "daily"
  BOT_INDEX_CACHE = "/app/content_cache/index_cache"
  BOT_REPO_PATH = "/app/content_cache/underworld3"

[http_service]
  internal_port = 8001
  force_https = true
  auto_stop_machines = true   # Stop when idle to reduce cost
  auto_start_machines = true
  min_machines_running = 1    # Temporarily 1 until index fully written to disk

  # HTTP service health checks
  [[http_service.checks]]
    grace_period = "300s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/health"

# Persistent volume for content cache
# Create with: flyctl volumes create helpfulbat_content --size 5 --region syd
[mounts]
  source = "helpfulbat_content"
  destination = "/app/content_cache"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 2048

# Secrets (set with flyctl secrets set)
# flyctl secrets set ANTHROPIC_API_KEY=sk-ant-...
